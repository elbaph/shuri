out/
node_modules/
dist/**/test/

.DS_Store
.vscode-test.*
*.local.*
*.map
*.vsix
Thumbs.db

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# mattpocock-skills per-repo config (issue tracker / triage / domain doc pointers) — kept local, not committed
/.claude/context/
